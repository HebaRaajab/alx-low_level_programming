#include "main.h"

/**
 * _abs - absuluate value of number
 * @n: integer to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
